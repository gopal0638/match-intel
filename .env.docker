# Docker environment configuration for local Postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cricket_db
DB_PORT=5432
# Variables for the Postgres image initialization
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cricket_db
# DB_HOST is not required for the container itself; when connecting from host use localhost
# Optional: set PGDATA if you want a custom data dir
# PGDATA=/var/lib/postgresql/data/pgdata
